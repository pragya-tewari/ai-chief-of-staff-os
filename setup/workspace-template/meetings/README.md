# Meetings

One derived note per meeting. The raw transcript or recording reference is the
immutable evidence; a note may be corrected if it misreads the source. Append a
dated change note explaining any correction. Everything downstream (tasks,
decisions, person updates) links back here.

The register at `registers/meetings.md` indexes them and is the duplicate gate.
