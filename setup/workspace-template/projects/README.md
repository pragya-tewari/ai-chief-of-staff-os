# Projects

One folder per project, each with an `index.md`: brief, owner, status, open
items, and links to its meetings and decisions. A project links to its tasks in
the task source — it never re-lists them.

Create a folder only when the work is live, time-bound, and has its own outcome.
Otherwise it stays a row in `org/portfolio-map.md`.
