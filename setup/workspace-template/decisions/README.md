# Decisions

One file per settled decision: what was decided, by whom, when, why, and what was
rejected. This is what stops the same thing being re-argued in six weeks.

Only settled things go here. A discussion that didn't settle stays in the meeting
note, or becomes an open question. Written by `manage-a-decision` (record mode),
and always needs a yes.
