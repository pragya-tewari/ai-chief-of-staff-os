# Run receipts

One Markdown checkpoint per logical run, named from the stable run key. Create the
receipt after the write plan/gate and before the first real write; update it after
every action using `schemas/run-receipt.md` from the product folder.

Link each receipt below in the same session it is created. A rerun reads its
existing receipt and destinations before acting.

- (none yet)
