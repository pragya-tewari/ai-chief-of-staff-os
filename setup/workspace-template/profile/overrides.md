# My overrides

> Operational rule layer, not a profile entity. Date each rule when it is added;
> this file does not use the entity header or a single global `updated` date.

Changes to the default behaviour, learned from how you actually work. The
assistant proposes additions here (you approve them). Product rules are never
edited — only this file is.

Each entry says what to do, and why, so it doesn't get undone by accident.

## Standing rules

- (none yet)

## Candidates (seen once or twice, not yet promoted)

- (none yet)
