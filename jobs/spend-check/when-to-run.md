# When to run

- `project money` — when a project's plan and its budget need reconciling,
  usually monthly or at a milestone.
- `recurring spend` — periodically, to catch tool and subscription spend that
  grows quietly.

Output is a report in `reports/`. It never edits a financial figure.
