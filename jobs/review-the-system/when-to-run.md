# When to run

Weekly. It's the job that keeps the system itself healthy — the one that notices
when the OS is drifting before the drift causes a real miss.

It reads the logs and the workspace and reports on the *system*, not the work.
It's also the fallback for the learning loop: it catches corrections the live
sessions missed.
