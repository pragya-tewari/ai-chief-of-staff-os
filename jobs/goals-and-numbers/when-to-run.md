# When to run

- `set` — at the start of a quarter, when goals are being agreed.
- `track` — monthly, to check progress and catch drift.
- `assemble` — when the recurring numbers pack is due (weekly or monthly).

The cadence in `org/cadence.md` says which is due when.
